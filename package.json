{
  "name": "svg-icon-system",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "npm run svg",
    "svg": "npm run optimise && npm run sprite",
    "optimise": "svgo -f src -r -o dist --config=svgo.json",
    "sprite": "rm ./dist/sprite.svg && svg-sprite --config sprite.json './dist/./**/*.svg'"
  },
  "dependencies": {
    "svg-sprite": "^1.5.0",
    "svgo": "^1.3.1"
  }
}
